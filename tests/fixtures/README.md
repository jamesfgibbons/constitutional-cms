# Synthetic Test Fixtures

**Provenance mode: synthetic**

Everything under `tests/fixtures/` is fabricated to exercise validators. Fixture URLs, values, statuses, and failure
states are not production observations and must not be cited as performance or conformance evidence.
